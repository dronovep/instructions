В файле /etc/httpd/conf/httpd.conf должна быть раскомментирована строка:
    LoadModule env_module modules/mod_env.so

В конфиге виртуального хоста директива:
  SetEnv ${название переменной} ${значение переменной(текстовая строка)}


